<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background with rounded corners -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" /> <!-- Background color -->
            <corners android:radius="12dp" /> <!-- Corner radius -->
        </shape>
    </item>

    <!-- Border -->
    <item android:right="2dp" android:left="2dp" android:top="2dp" android:bottom="2dp">
        <shape android:shape="rectangle">
            <solid android:color="#00000000" /> <!-- Transparent color for the center -->
            <stroke android:width="2dp" android:color="#FF0000" /> <!-- Border color and width -->
            <corners android:radius="10dp" /> <!-- Adjust the corner radius to match the background -->
        </shape>
    </item>
</layer-list>
